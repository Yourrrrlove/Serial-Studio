{
  "purpose": "Out-of-graph includes each ratcheted core/ layer carries today. Empty since spec 0077: every layer is strict, so an out-of-graph include is an error, not debt. scripts/layer-verify.py fails when an edge grows past its count.",
  "regenerate": "python3 scripts/layer-verify.py --accept",
  "generated": "2026-09-09",
  "edges": {}
}
